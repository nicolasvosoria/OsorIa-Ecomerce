# Skill Registry

## Project Conventions

- No project-specific standards were found.
- Scanned root instruction files: none (`AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `copilot-instructions.md`, `.cursorrules`).

## Available User Skills

- `branch-pr` — PR creation workflow (trigger: creating/opening a PR)
- `go-testing` — Go testing patterns (trigger: Go tests / Bubbletea TUI testing)
- `issue-creation` — GitHub issue workflow (trigger: creating an issue / reporting a bug / requesting a feature)
- `judgment-day` — dual review protocol (trigger: "judgment day" / adversarial review requests)
- `skill-creator` — create new AI agent skills (trigger: creating or documenting a new skill)

## Notes

- SDD skills (`sdd-*`) and shared/internal skills were intentionally excluded from this registry.
